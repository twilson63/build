name = "build"
version = "0.1.0"
target = "javascript"

# Gleam/Lustre prototype for the browser frontend migration.

[dependencies]
gleam_stdlib = ">= 1.0.0 and < 2.0.0"
lustre = ">= 5.7.0 and < 6.0.0"

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
